---
const currentYear = new Date().getFullYear();

const categories = [
  { name: 'Freelance', href: '/freelance' },
  { name: 'Creator', href: '/creator' },
  { name: 'Side Hustle', href: '/side-hustle' },
];

const calculators = [
  { name: 'Hourly Rate', href: '/freelance/hourly-rate-calculator' },
  { name: 'YouTube AdSense', href: '/creator/youtube-adsense-calculator' },
  { name: 'Time to Goal', href: '/side-hustle/time-to-goal-calculator' },
];
---

<footer class="bg-gray-900 text-gray-300">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
      <div class="md:col-span-2">
        <span class="text-2xl font-bold text-white">Calcverse</span>
        <p class="mt-3 text-gray-400 max-w-md">
          Free calculators for freelancers, creators, and side hustlers.
          Make smarter financial decisions with our simple, accurate tools.
        </p>
      </div>

      <div>
        <h3 class="text-white font-semibold mb-3">Categories</h3>
        <ul class="space-y-2">
          {categories.map((cat) => (
            <li>
              <a href={cat.href} class="hover:text-white transition-colors">
                {cat.name}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div>
        <h3 class="text-white font-semibold mb-3">Popular Calculators</h3>
        <ul class="space-y-2">
          {calculators.map((calc) => (
            <li>
              <a href={calc.href} class="hover:text-white transition-colors">
                {calc.name}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <div class="mt-10 pt-8 border-t border-gray-800 text-center text-gray-400 text-sm">
      <p>&copy; {currentYear} Calcverse. All rights reserved.</p>
    </div>
  </div>
</footer>
